import(dplyr, except = combine)
import(ggplot2)
import(tidyr)
import(cowplot)
import(ggtext)
import(glue)
import(gridExtra)
import(parallel)
import(pheatmap)
import(mclust, except = map)
import(aricode, except = entropy)
import(cluster)
import(clusterSim)
import(entropy)
import(reshape2)
import(viridis, except = viridis_pal)
import(transport)
import(digest)
import(readr)
import(FNN, except = entropy)
import(foreach, except = c(when, accumulate))
import(doParallel)
import(processx)
import(ps)
import(purrr)
import(rlang, except = c("%@%", flatten_lgl, splice, flatten_chr, flatten_raw, flatten, flatten_dbl, invoke, flatten_int))
import(scales, except = c(col_factor, discard))
import(umap)
import(ripserr)
import(TDA, except = c(wasserstein, silhouette))
import(TDAstats)
import(ComplexHeatmap, except = pheatmap)
import(circlize)
import(dendextend)
import(Seurat)
import(SeuratDisk)
import(kernlab, except = c(cross, alpha))
import(RColorBrewer)
import(svglite)
import(Matrix, except = c(unpack, expand, pack))
importFrom(grDevices, dev.off, pdf)
importFrom(graphics, text)
importFrom(grid, gpar, grid.newpage, grid.text)
importFrom(methods, new, slotNames)
importFrom(stats, approx, as.dendrogram, as.dist, dist, hclust, kmeans, ks.test, order.dendrogram, p.adjust, prcomp, quantile, sd, setNames)
importFrom(utils, capture.output, object.size, read.csv, str, write.csv, write.table)
export(run_unified_pipeline)
export(run_postprocessing_pipeline)
export(process_datasets_PH)
export(run_modular_analysis)
export(run_cross_iteration)
export(load_custom_iteration_inputs_template)
export(import_custom_iteration_inputs)

export(perform_integration)
export(generate_toy_data)
export(run_toy_baseline)
export(run_realistic_fixture)
