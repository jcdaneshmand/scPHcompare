# MV5-AB label-closed cosine configuration execution

Date: 2026-08-11

Authorization base: `89be63d`

Prospective engine commit: `20ec50e`

Runtime/source binding commit: `6b37eac`

## Outcome

MV5-AB completed and accepted exactly the frozen
`cells384_pc30_cosine_chord_v1` calculation. All 150 groups finished on their
first production pass. No label, ranking, outcome, comparison, clustering, or
nested-cell calculation was performed.

The accepted MV5-U/W scientific primitives already implemented the required
cosine transform: reject zero/nonfinite row norms, row-L2-normalize the 384 by
30 accepted training-fit coordinates, and use Euclidean chord geometry. The
new prospective layer only binds the cosine-specific queue, implementation and
runtime identities, one-configuration resource caps, monitor, and firewall.

## Complete calculation axes

- 150/150 groups completed;
- 13,500 normalized cell views;
- 27,000 separate H0/H1 landscape summaries;
- 70,700 directed heldout-query/training-reference biological pairs;
- 141,400 exact all-active H0/H1 landscape-distance rows in 720 subchunks;
- 70,700 matched-energy rows; and
- 282,800 four-method label-closed rows.

All views retain 384 cells and all 30 accepted coordinates. No PCA refit,
coordinate truncation, cell reselection, or altered landscape definition was
used.

## Resources

The first production pass used 9,389.525 aggregate monitored seconds (2.608
worker-hours). Maximum group time was 151.659 seconds, maximum process-tree RSS
was 615,645,184 bytes, and the completed private result root was 2,480,675,408
bytes. These pass the frozen 600-second/4-GiB group, 8-worker-hour, and 4-GiB
configuration caps.

## Independent validation

The structural/artifact validator passed 15 categories. It revalidated all 179
bound sources, 150 queue identities, 150 atomic group publications, 1,350
manifest entries, 1,650 private files, exact row counts, all-active/no-cap
landscape flags, resource caps, and zero-label/zero-outcome closure. It
reconstructed all 282,800 method rows from H0, H1, raw-composite, and energy
components.

A separately written raw-coordinate validator called no production scientific
helper. It independently:

- loaded every accepted SCT/integrated coordinate source;
- rejected any nonfinite or nonpositive source norm;
- row-normalized all 13,500 views;
- reconstructed all 5,170,500 finite H0 MST deaths with maximum absolute error
  `5.10702591327572e-15`; and
- directly recalculated 30 energy distances across minimum/median/maximum fold
  sizes, both representations, and all five seeds, with maximum absolute error
  `4.88498130835069e-15`.

Both validation layers were rerun into a clean root. All nine generated ledgers
reproduced byte-for-byte.

## Repeat and immutable resume

The two prospectively marked maximum-training-fold groups—one per
representation at seed `20260805`—were rebuilt in a separate private root. All
16 deterministic scientific artifacts match byte-for-byte.

A full validation-only resume reused all 150 groups. Before/after snapshots are
identical for all 1,650 relative paths, byte sizes, SHA-256 hashes, and
modification timestamps. No file was rebuilt or touched.

## Decision boundary and next action

The cosine calculation is accepted only as a label-closed robustness input.
Its biological effect is unknown because rankings and labels have not been
opened. No comparison with PC30 Euclidean or PC20, equivalence claim, method
selection, or default change is permitted from this sprint.

The next reasonable sprint is a separate cosine retrieval-outcome prefreeze.
It should prove exact baseline/cosine query-reference-method pairing, freeze the
complete direct and topology-increment estimands and blocked uncertainty, and
commit a prediction lock before tissue access. Cosine clustering remains
non-identifiable because this calculation contains directed heldout-training
pairs only. Both nested-cell configurations remain closed pending another
selection-resistant continuation gate.
