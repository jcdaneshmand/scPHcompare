# MV8-SB execution-head recovery prefreeze v1

The first MV8-SA replacement launch supplied a manually expanded 40-character
hash that did not equal the actual Windows Git HEAD. The mismatch was caught
immediately. The parent was interrupted, its exact orphan child was terminated,
and the v2 roots contain only two empty logs: no public ledger, baseline output,
PH record, or landscape artifact.

One replacement may run in fresh v3 roots after this amendment is committed.
The full execution head must be captured directly from `git rev-parse HEAD` and
passed to both runner fields in the same PowerShell launch command. The v1 and
v2 roots remain immutable.

The scientific and resource contracts are unchanged. Landscapes remain closed
under the separate-H0/H1, all-finite-positive, essential-H0-excluded,
all-active-level, exact/error-controlled, no-grid, no-level-cap streamed
definition. Full PH, comparisons, clustering, fusion, labels, outcomes,
adoption, and manuscript claims remain closed.
