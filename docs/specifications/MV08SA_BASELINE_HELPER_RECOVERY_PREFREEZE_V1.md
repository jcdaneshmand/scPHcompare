# MV8-SA baseline-helper recovery prefreeze v1

MV8-S execution commit `218af2869e4664bbaa020fe4fed03075f601a849`
stopped on the first HCA_BM_001 baseline before cell selection. The child could
not resolve `.with_preserved_seed` because it did not source `R/toy_baseline.R`.
The monitored receipt records exit 1 after 32.7 seconds at 0.93 GB, with no
baseline output, no PH, and no landscape computation.

The only remediation is to source the existing helper before the dual-view
code. One clean replacement of the complete 66-unit sentinel may run in fresh
private/public roots after this amendment is committed. The failed roots are
immutable evidence. The replacement still has one worker, zero within-run
retries, the exact original resource/fallback policy, and all original
scientific axes and definitions.

In particular, landscapes remain closed and retain separate H0/H1, all finite
positive intervals, essential-H0 exclusion, every consecutive active level,
exact or error-controlled squared-L2 integration, no fixed grid, no level cap,
and streamed/chunked computation. Full PH, comparisons, clustering, fusion,
labels, outcomes, adoption, and manuscript claims remain closed.
