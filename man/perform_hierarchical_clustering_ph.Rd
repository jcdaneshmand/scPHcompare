% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PH_PostProcessing_andAnalysis.R
\name{perform_hierarchical_clustering_ph}
\alias{perform_hierarchical_clustering_ph}
\title{Perform hierarchical clustering on a Bottleneck distance matrix}
\usage{
perform_hierarchical_clustering_ph(
  bdm_matrix,
  k,
  methods = c("ward.D2", "average", "complete", "mcquitty"),
  verbose = TRUE
)
}
\arguments{
\item{bdm_matrix}{Square numeric matrix of Bottleneck distances where
rows and columns share the same sample ordering.}

\item{k}{Integer number of clusters to cut from each dendrogram.}

\item{methods}{Character vector of hierarchical clustering linkage
methods to evaluate (passed to `hclust()`).}

\item{verbose}{Logical, print progress messages if `TRUE`.}
}
\value{
A list with elements:
\describe{
  \item{results}{Named list of linkage methods, each containing
  `clusters` (a vector of cluster assignments) and `tree` (an `hclust`
  object).}
  \item{clusters}{Cluster assignments from the first requested
  method for convenience.}
  \item{tree}{The `hclust` tree from the first requested method.}
}
}
\description{
Wrapper around `hclust()` that validates a Bottleneck distance
matrix (BDM), applies one or more linkage methods and returns the
resulting dendrograms and cluster assignments. Cluster labels are
ordered according to the sample names in the distance matrix.
}
\keyword{internal}
